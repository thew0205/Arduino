void setup() {
  // put your setup code here, to run once:
  //type variable;
  Serial.begin(9600);
}

int delayTime = 0;
int feranmi = 0;
char A = '+';
bool damilare = false;
char juwonlo = 'A';
float matthew = 0.5;
int matthias;
String myName = "Busoye Matthew. I am teaching a very silly girl and she knows herself. ";
int theOnLED = 0;
void loop() {
  // put your main code here, to run repeatedly:
  Serial.print("feranmi :");
  Serial.println(feranmi);
  Serial.print("damilare :");
  Serial.println(damilare);
  Serial.print("juwonlo :");
  Serial.println(juwonlo);
  Serial.print("matthew :");
  Serial.println(matthew);
  Serial.print("matthias :");
  Serial.println(matthias);
  delay(5000);
  feranmi = feranmi + 1;
  damilare = true;
  matthias = feranmi;
  int the_address_where_the_word_is;
  bool should_go = false;
//variable_type variable_name;
int mat 
}


//
//
//void setup() {
//  // put your setup code here, to run once:
//  Serial.begin(9600);
//  for (int i = 0 ; i < 5; i ++)
//    pinMode(13 - i, OUTPUT);
//}
//
//int delayTime = 0;
//
//int theOnLED = 0;
//void loop() {
//  // put your main code here, to run repeatedly:
//  for (int i = 0 ; i < 5; i ++)
//    digitalWrite(13 - i, LOW);
//  digitalWrite(13 - theOnLED, HIGH);
//
//  delay(3000);
//  delayTime =  (delayTime + 1) % 100;
//  theOnLED =  (theOnLED + 1) % 5;
//
//
//}
